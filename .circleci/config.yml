version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
      - image: circleci/android:api-26-alpha
    environment:
    - ANDROID_HOME: '/opt/android/sdk'
    steps:
    - checkout
    - run:
        name: npm-install
        command: 'npm install'
    - run:
        name: build-android
        command: 'ls /opt/'
