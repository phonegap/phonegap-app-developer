version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-26-alpha
      - image: circleci/node:latest
    environment:
    - ANDROID_HOME: '/opt/android/sdk'
    steps:
    - checkout
    - run:
        name: npm-install
        command: 'npm install'
    - run:
        name: build-android
        command: 'ls /opt/'
