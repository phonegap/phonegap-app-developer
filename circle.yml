machine:
  node:
    version: 4.0.0

general:
  artifacts:
    - "/home/ubuntu/phonegap-app-developer/platforms/android/build/outputs/apk/"

deployment:
  production:
    branch: continuous-integration
    commands:
      - pip install awscli
      - bash resources/circle-ci/android/aws-upload.sh
      - npm run distribute-android-ad-hoc -- $HOCKEY_APP_TOKEN $HOCKEY_APP_ANDROID_ID $CIRCLE_SHA1
